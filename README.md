# Nuxt 3 + Pharen UI Example

This project is a minimal example of a Nuxt 3 frontend application using the `@pharen/ui` UI kit.

## Setup

1.  Install dependencies:

    ```bash
    cd frontend
    npm install
    ```

2.  Run the development server:

    ```bash
    npm run dev
    ```

3.  Open your browser and navigate to `http://localhost:3000/calculator`.

## Calculator

The application includes a simple calculator component located at `/calculator`.
It takes two numeric inputs and provides buttons for basic arithmetic operations (+, -, ×, ÷).

